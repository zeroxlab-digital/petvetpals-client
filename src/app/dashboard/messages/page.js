import Messenger from '@/components/Common/Messenger/Messenger';
import React from 'react';

const page = () => {
    return (
        <div>
            <Messenger />
        </div>
    );
};

export default page;