import Appointments from '@/components/Common/Appointments/Appointments';
import React from 'react';

const page = () => {
    return (
        <div>
            <Appointments />
        </div>
    );
};

export default page;